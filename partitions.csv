# Name,   Type, SubType, Offset,   Size,     Flags
# Two OTA app slots (app0/app1) added 2026-07-26 for the planned OTA-update
# feature -- app0 was already typed ota_0 even before this (unclear why,
# but it meant the OTA-selection plumbing (otadata) was already half in
# place). Reclaimed from the old single 4MB app0 + 11.94MB spiffs -- spiffs
# was allocated but confirmed completely unused (no code anywhere mounts or
# references it; it was presumably reserved for the tile-cache feature,
# which is disabled due to an ESP32-P4 PPA hardware bug, not just
# unimplemented). 3MB per app slot is ~50% headroom over the current
# ~2MB firmware -- if a future build ever grows past that, both slots need
# resizing together (and existing installs will need a full reflash, not
# just an OTA, since this changes the partition table itself). coredump
# kept as-is -- genuinely useful given this project's real crash history.
# ~9.9MB is intentionally left unallocated ("use what you need," not
# padded out for a use that doesn't exist yet) -- easy to claim later for
# an actual need (e.g. re-enabling tile cache) without disturbing the app
# slots, since it's untouched free space at the end of the flash.
nvs,      data, nvs,     0x9000,   0x5000,
otadata,  data, ota,     0xe000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x300000,
app1,     app,  ota_1,   0x310000, 0x300000,
coredump, data, coredump,0x610000, 0x10000,
