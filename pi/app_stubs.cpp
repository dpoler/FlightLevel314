// Temporary placeholders for subsystems that haven't been ported to Linux
// yet. (metar / atis live in platform_linux/*_linux.cpp.)

// Intentionally empty — kept so CMake can retain app_stubs.cpp as a
// catch-all for future thin stubs without dropping the translation unit.
